<UserControl x:Class="Presentation.WPF.Views.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.WPF.Views"
             xmlns:viewmodels="clr-namespace:Presentation.WPF.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:UserViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="980.677" d:DesignWidth="800">
    <Grid Margin="20">

        <StackPanel>

            <Label Content="FirstName" />
            <TextBox Text="{Binding FirstName}" Margin="0,0,0,20"></TextBox>

            <Button Content="Add User" Command="{Binding AddToListCommand}"/>

        </StackPanel>
            
    </Grid>
</UserControl>
